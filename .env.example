# Database Configuration
DB_NAME=time_manager_db
DB_USER=postgres_dev
DB_PASSWORD=change_me_in_production
DB_HOST=db
DB_PORT=5432

# Exposed Ports (for local dev and CI/CD)
DB_PORT_EXTERNAL=5432
BACKEND_PORT_EXTERNAL=8081
FRONTEND_PORT_EXTERNAL=8080

# Nginx Configuration
NGINX_PORT=80

# Production only
VERSION=1.0.0
BUILD_NUMBER=1