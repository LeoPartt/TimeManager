# ========================================
# PRODUCTION CONFIGURATION
# ========================================
# These are used by compose.prod.yaml and CI/CD pipelines
# In production, use proper secrets management (Vault, AWS Secrets Manager, etc.)
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file with real credentials!

# Database Configuration
DB_NAME=timemanager_prod
DB_USER=timemanager_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_HOST=db
DB_PORT=5432

# Server Configuration (optional, for future use)
# SERVER_HOST=your-domain.com
# SERVER_PORT=8080
# SSL_ENABLED=true
